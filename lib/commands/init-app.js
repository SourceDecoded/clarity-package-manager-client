/**
 * init-app.js
 * tool for stubbing out apps and components, etc.
 */

module.exports = function(env, options) {
  var templateRoot = "../templates/app/";

  // basically copy the files from the template root into the current directory
  



}
